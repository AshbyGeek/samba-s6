#!/usr/bin/with-contenv sh

# Disabled these because anonymous access was working fine until I fixed things so that they actually had effect. :/
#addgroup -g $GID $GROUPNAME
#adduser -S -G $GROUPNAME -u $UID -H -D $USERNAME

#echo "$PASSWORD" | tee - | smbpasswd -s -c /etc/samba/smb.conf -a $USERNAME
